// Fetch assert
const assert = require('assert');

// Start a unit test group
describe('myapp', () => {

    // Our first test
    it('true must be true', () => {
        assert.strictEqual(true, true);
    });

});
